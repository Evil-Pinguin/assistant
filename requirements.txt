# --- Интерфейс ---
PySide6>=6.6            # Command Center (окно, лицо, вкладки)

# --- Ядро ---
requests>=2.28          # погода, ИИ, плагины
psutil>=5.9             # статус системы, «почему тормозит», закрытие приложений

# --- Голос ---
SpeechRecognition>=3.10 # распознавание речи
pyttsx3>=2.90           # озвучка (Windows SAPI5 / Linux espeak-ng)
PyAudio>=0.2.13         # микрофон

# --- Автоматизация и действия ---
pyautogui>=0.9.54       # клавиши, клики, печать, громкость
pyperclip>=1.8          # буфер обмена
Pillow>=9.0             # скриншоты и зрение
pynput>=1.7             # Ctrl+Space (говорить), Teach Mode, запись макросов

# --- Опционально: офлайн-распознавание ---
# pip install vosk  →  модель с https://alphacephei.com/vosk/models
# vosk>=0.3.45
